// Skeleton page. The Noon Poster experience (design tokens, living status zone,
// Buy Now flow + verdicts) lands in Epic 2 against the Epic 1 API contract.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Flash Sale</h1>
      <p>Keycap·One — the sale page arrives with Epic 2.</p>
    </main>
  );
}
